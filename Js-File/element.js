

// function decrements(){
//     const counterElement = document.getElementById("numbers");
//     let value =parseInt(counterElement);
//     console.log(typeof );
//     value = value -1;
//     counterElement.innertext = value;
   
  
// }
// task assign decrement
    document.getElementById("complete-btn-one").addEventListener('click',
        function(){
        const decrement= document.getElementById("numbers");
        let value = parseInt(decrement.innerText);
        if(value>0){
            value = value -1;
            
        }
        decrement.innerText =value;
       
    
        }
    )

    document.getElementById("complete-btn-two").addEventListener('click',
        function(){
        const decrement= document.getElementById("numbers");
        let value = parseInt(decrement.innerText);
        if(value>0){
            value = value -1;
            
        }
        decrement.innerText =value;
    
        }
    )

    document.getElementById("complete-btn-three").addEventListener('click',
        function(){
        const decrement= document.getElementById("numbers");
        let value = parseInt(decrement.innerText);
        if(value>0){
            value = value -1;
            
        }
        decrement.innerText =value;
    
        }
    )

    document.getElementById("complete-btn-four").addEventListener('click',
        function(){
        const decrement= document.getElementById("numbers");
        let value = parseInt(decrement.innerText);
        if(value>0){
            value = value -1;
            
        }
        decrement.innerText =value;
    
        }
    )

    document.getElementById("complete-btn-five").addEventListener('click',
        function(){
        const decrement= document.getElementById("numbers");
        let value = parseInt(decrement.innerText);
        if(value>0){
            value = value -1;
            
        }
        decrement.innerText =value;
    
        }
    )

    document.getElementById("complete-btn-six").addEventListener('click',
        function(){
        const decrement= document.getElementById("numbers");
        let value = parseInt(decrement.innerText);
        if(value>0){
            value = value -1;
            
        }
        decrement.innerText =value;
    
        }
    )

    // increment section

    // first

    document.getElementById('complete-btn-one').addEventListener('click', function(){
        const increment = document.getElementById('position');
        let values = parseInt(increment.innerText);
        if(values>=23){
            values= values + 1;
        }

        increment.innerText = values;
    })
//    second
    document.getElementById('complete-btn-two').addEventListener('click', function(){
        const increment = document.getElementById('position');
        let values = parseInt(increment.innerText);
        if(values>=23){
            values= values + 1;
        }

        increment.innerText = values;
    })
// third
    document.getElementById('complete-btn-three').addEventListener('click', function(){
        const increment = document.getElementById('position');
        let values = parseInt(increment.innerText);
        if(values>=23){
            values= values + 1;
        }

        increment.innerText = values;
    })
//   fourth
    document.getElementById('complete-btn-four').addEventListener('click', function(){
        const increment = document.getElementById('position');
        let values = parseInt(increment.innerText);
        if(values>=23){
            values= values + 1;
        }

        increment.innerText = values;
    })
// fifth
    document.getElementById('complete-btn-five').addEventListener('click', function(){
        const increment = document.getElementById('position');
        let values = parseInt(increment.innerText);
        if(values>=23){
            values= values + 1;
        }

        increment.innerText = values;
    })
// six
    document.getElementById('complete-btn-six').addEventListener('click', function(){
        const increment = document.getElementById('position');
        let values = parseInt(increment.innerText);
        if(values>=23){
            values= values + 1;
        }

        increment.innerText = values;
    })


